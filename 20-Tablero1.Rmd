---
title: "Covid-19 Colombia"
output: 
  flexdashboard::flex_dashboard:
    orientation: columns
    vertical_layout: fill
---

```{r setup, include=FALSE}
library(tidyverse)
source("00-configuracion_inicial.R")
source("10-obtencion_datos.R")
```

Column {data-width=600}
-----------------------------------------------------------------------

### Reporte a la fecha

```{r}
tabla
```

Column {data-width=650} 
-----------------------------------------------------------------------

### Fallecidos por departamento 

```{r}
mapa$casa=tabla[,1]
mapa$fall=tabla[,2]
mapa$hosp=tabla[,3]
mapa$uci=tabla[,4]
mapa$recup=tabla[,6]
mapa$inf=tabla[,1]+tabla[,3]+tabla[,4]+tabla[,5]
# col <- rev(heat.colors(26))
# spplot(mapa[,7],
#       col.regions=col)
mapview(mapa[,7],layer.name="Fallecidos")

```
